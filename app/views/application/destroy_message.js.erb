var msgEl = $('#m_' + "<%= escape_javascript(@id) %>")
if (msgEl.length) {
	msgEl.removeClass('animate__animated animate__backInUp');
	msgEl.fadeOut('slow', function() {
    	msgEl.remove();
	});	
};
Turbolinks.clearCache()